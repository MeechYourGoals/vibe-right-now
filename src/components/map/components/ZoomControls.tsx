
import React from 'react';
import { useMap } from 'react-leaflet';

const ZoomControls: React.FC = () => {
  // This component doesn't render any UI elements directly
  // It just adds zoom controls to the existing Leaflet map
  // This is a workaround since we disabled default controls for better positioning
  return null;
};

export default ZoomControls;
